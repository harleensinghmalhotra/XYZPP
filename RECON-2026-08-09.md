# RECON-2026-08-09 — Read-only state audit

Read-only. No source files were modified. The only file created is this report.
`pnpm build` (which rewrites the git-ignored `dist/`) and `vite preview` were run as
instructed; neither touches tracked files.

**Method key.** Every claim is tagged:
- **[SHOT]** = derived from a real screenshot that was opened and visually read. Screenshot
  files live in the session scratchpad at
  `…/ddf39d07-…/scratchpad/shots/` (referenced by filename below).
- **[RUNTIME]** = read from the live DOM in the production preview (e.g. `document.title`,
  bounding rects, `[data-reveal]:not(.is-in)` counts). Not a screenshot, but observed in the
  running production build, not grepped.
- **[GREP]** = from static search of source. **Unverified** per the brief, especially for
  `site-assets/` (dynamic path templates look unreferenced to grep).
- **[CODE]** = read directly out of a source file.
- **[BUILD]** = from the `pnpm build` / preview run.

Preview served the production `dist/` at `http://localhost:4173` via `vite preview`.
Desktop canonical viewport 1536×743 @ DPR 1.25; mobile 390×844 @ DPR 3.

---

## 1. Repo and build state

**Branch / HEAD.** [CODE/BUILD]
- Current branch: `phase2-routing`.
- HEAD sha: `5a664e87f0b25dc6f2a4ec29566a7e48c16ff63f` (`5a664e8`).
- **HEAD matches `5a664e8`.** No commits have been made on top of it; nothing to list.

**Remote / tracking.** [CODE]
- `origin` = `https://github.com/harleensinghmalhotra/XYZPP.git` (fetch and push). Matches
  the expected `harleensinghmalhotra/XYZPP`.
- Upstream = `origin/phase2-routing`. **0 ahead / 0 behind** origin.

**`pnpm build`.** [BUILD]
- Result: **SUCCESS** (exit 0). `vite v5.4.21`, 7200 modules transformed, "built in 21.59s";
  total wall time `real 0m26.794s`.
- **Warnings:** one — the chunk-size warning ("Some chunks are larger than 500 kB after
  minification") triggered by three chunks: `index-BIl81377.js` (1135.74 kB),
  `maplibre-gl-CCHxMX9b.js` (1053.93 kB), `react-globe.gl-Dg8zoFPx.js` (1912.68 kB).
  No errors.
- **Main bundle hashes (this build):**
  - JS: `dist/assets/index-BIl81377.js` — 1,139,755 bytes (gzip 361.04 kB)
  - CSS: `dist/assets/index-22rJt-bd.css` — 271,531 bytes (gzip 52.02 kB)
  - other emitted chunks: `maplibre-gl-CCHxMX9b.js`, `react-globe.gl-Dg8zoFPx.js`,
    `browser-6VweCCht.js`, `stegaEncodeSourceMap-Bha_dJl4.js`, `maplibre-gl-Twyd4PtP.css`.
- **Total `dist/` size:** 90,550,289 bytes (≈ 86.4 MiB / "88M").

**Hash comparison vs deployed (`index-BIl81377.js` / `index-22rJt-bd.css`).** [BUILD]
- **IDENTICAL — no drift.** The freshly built main JS and CSS carry the exact same Vite
  content-hash filenames as the deployed build (`index-BIl81377.js`, `index-22rJt-bd.css`).
  Same content hash ⇒ same bytes. The current source produces the deployed artifacts.
  (The pre-existing `dist/` on disk before the rebuild already carried these same two
  filenames, dated Jul 30 10:09.)

**Uncommitted / untracked files.** [CODE]
- **No modified tracked files.** Working tree is clean apart from the pre-existing untracked
  items below (`git status` start == end — see the two captures).
- Untracked (present at start, unchanged at end):
  `FINAL ASSETS ARE HERE.zip`, `NEW QFP AV.mp4`, `THE FINAL DESKTOP WEBSITE CHANGE/`,
  `WhatsApp Image 2026-07-29 at 1.12.52 PM.jpeg`, `_assets-in/`, `_assets-in2/`,
  `_lane1/`…`_lane7/`, `_recon/`, `_recon2/`, `_recon3/`, `drive-download-20260728T032950Z-1-001.zip`.
- `dist/` was rebuilt but is git-ignored (`.gitignore`), so it does not appear in status.

**git status at START:**
```
?? "FINAL ASSETS ARE HERE.zip"
?? "NEW QFP AV.mp4"
?? "THE FINAL DESKTOP WEBSITE CHANGE/"
?? "WhatsApp Image 2026-07-29 at 1.12.52 PM.jpeg"
?? _assets-in/  _assets-in2/  _lane1/ _lane2/ _lane3/ _lane4/ _lane5/ _lane6/ _lane7/
?? _recon/  _recon2/  _recon3/
?? drive-download-20260728T032950Z-1-001.zip
```
**git status at END:** identical to the above (verified with `git status --porcelain`; only
addition is this report file `RECON-2026-08-09.md`, which is untracked and intended).

---

## 2. Route inventory

Router = `src/App.jsx` (`<Routes>` under one `<SiteLayout>`). [CODE] Every route below was
deep-linked on the preview server, scrolled top-to-bottom to fire reveals, and screenshotted
full-page at 1536×743 DPR 1.25. **Console/page errors observed: 0 on every route** [RUNTIME].
`[data-reveal]:not(.is-in)` after settle = **0 on every route**; suspicious hidden blocks
(>150px tall at opacity<0.05 / visibility:hidden) = **0 on every route** [RUNTIME].

| Path | Component | Render verdict |
|---|---|---|
| `/` | `Home` | **Complete** — all 13 sections render. [SHOT route-home.png] |
| `/about` | `OurStory` | **Complete**; one caveat (animated timeline band, see below). [SHOT route-about.png] |
| `/global-markets` | `GlobalMarkets` | **Complete**; contains an "Our Global Team — Details coming shortly" placeholder block. [SHOT route-global-markets.png] |
| `/educational-books` | `<Navigate to="/#wwp-educational">` | Redirects to homepage WWP anchor. [RUNTIME] |
| `/trade-books` | `<Navigate to="/#wwp-trade">` | Redirects to homepage WWP anchor. [RUNTIME] |
| `/print-on-demand` | `PrintOnDemand` | **Complete** — configurator + Explore Categories. [SHOT route-print-on-demand.png] |
| `/infrastructure` | `InfrastructurePage` | **Complete**. [SHOT route-infrastructure.png] |
| `/newsroom` | `Newsroom` | **Complete** — 6 live Sanity cards. **3 older cards (2022/2022/2017) render with no cover image** (blank media panel). [SHOT route-newsroom.png] |
| `/newsroom/:slug` | `NewsroomArticle` | **Complete** (tested `printweek-power-100-2026`). [SHOT route-newsroom-article.png] |
| `/fulfilment` | `Fulfilment` | **Complete**. [SHOT route-fulfilment.png] |
| `/contact` | `Contact` | **Complete** — enquiry form + 16-item FAQ. [SHOT route-contact.png] |
| `/legal/privacy` | `LegalPage doc="privacy"` | **Complete**. [SHOT route-legal-privacy.png] |
| `/legal/cookies` | `LegalPage doc="cookies"` | **Complete** (same template; 0 errors). [RUNTIME] |
| `/legal/terms` | `LegalPage doc="terms"` | **Complete** (same template; 0 errors). [RUNTIME] |
| `/legal/accessibility` | `LegalPage doc="accessibility"` | **Complete** (same template; 0 errors). [RUNTIME] |
| `*` (e.g. `/this-route-does-not-exist-xyz`) | `NotFound` | **Complete** — "This page is out of print." 404 page. [SHOT route-notfound-404.png] |

**Nothing 404s or blanks.** Findings to flag:
- `/global-markets` "Our Global Team — Details coming shortly." is a live placeholder/stub
  section (renders copy, no roster). [SHOT route-global-markets.png]
- `/newsroom` — the three oldest cards have empty cover-image panels. [SHOT route-newsroom.png]
- `/about` — the "JOURNEY TIMELINE" band reads as a large near-empty area in a static capture;
  it is an animation-driven section, not a blank (see §3 and §7 notes). [SHOT about-prun.png]
- **Navigation orphans:** `to="/global-markets"` and `to="/fulfilment"` appear **nowhere** in
  `src` [GREP]. Both routes exist and are in the sitemap but have no in-app inbound link;
  they are reachable only by typing the URL.
- `EducationalBooks.jsx` / `TradeBooks.jsx` page components exist but their routes are
  redirects to homepage anchors, so those components never render. [CODE]

---

## 3. The ScrollTrigger landmine — audit only

**`smooth-scroll.jsx` is untouched and still contains the killAll.** [CODE]
`src/lib/smooth-scroll.jsx:62` `ScrollTrigger.killAll()` in the provider-unmount cleanup, plus
a mount-time `ScrollTrigger.getAll().forEach((t) => t.kill())` at line 33. Not removed.

**Hide-first GSAP reveal patterns still in the codebase** (`gsap.set(…, {autoAlpha:0/opacity:0})`
before a `.to(…, {autoAlpha:1})`, or `gsap.from(…, {autoAlpha:0, scrollTrigger})`). [GREP —
unverified]:
- `src/sections/Sustainability.jsx` — lines 69–80 (`gsap.set` autoAlpha:0 on title/intro/media/item; `gsap.from(.svA-chip, autoAlpha:0, scrollTrigger once:true)`).
- `src/sections/Projects.jsx` — lines 266–297 (extensive `gsap.set` autoAlpha:0 + timeline `.to` autoAlpha:1).
- `src/sections/Infrastructure.jsx` (the homepage **section**) — lines 126–149.
- `src/sections/ProcessVideo.jsx` — lines 68–72 (`.fromTo` autoAlpha:0 → 1).

Patterns that are **NOT** content hide-first reveals (excluded): `Contact.jsx`,
`Fulfilment.jsx`, `EducationalBooks.jsx` use `gsap.set(p, {strokeDasharray…})` — SVG
line-draw only; `JourneyTimeline.jsx` uses the Web Animations API (`element.animate`,
`opacity:0` keyframes), not GSAP/ScrollTrigger — immune to `killAll`.

**Cross-reference against the five previously-fixed sections** (Awards, Certifications,
Infrastructure, Contact, Fulfilment): none of those appear in the grep above — `Awards.jsx`
and `Certifications.jsx` no longer use `autoAlpha` at all; `InfrastructurePage.jsx`,
`Contact.jsx`, `Fulfilment.jsx` carry only SVG line-draws. So the fixes are intact. [GREP]

**Are the four remaining hide-first sections a new landmine?** They are imported **only by
`Home.jsx`** [GREP]:
- `Sustainability`, `Projects`, `Infrastructure` (section), `ProcessVideo` → imported by
  `src/pages/Home.jsx` and nowhere else.
- Inner pages import only `Awards` + `Certifications` (both fixed) and `JourneyTimeline`
  (WAAPI): `InfrastructurePage.jsx` (Awards, Certifications), `OurStory.jsx` (Awards,
  Certifications, JourneyTimeline).
So every remaining hide-first ScrollTrigger reveal lives on the **homepage only** — on the page
that *owns* the `SmoothScrollProvider`. None is reused on a killAll-victim inner page. **No new
landmine on any inner route** in code.

**Browser proof (production preview, home first, then click through — not deep-linked).**
For each inner route reachable by a real SPA click from the homepage, the homepage was loaded,
its engine confirmed booted (`window.__lenis` present), the link clicked, and — after the
navigation — `window.__lenis` confirmed **gone** (i.e. the homepage unmounted and the
`killAll` DID fire). Result per route [RUNTIME + SHOT nav-*.png]:

| home → click → route | killAll fired (`lenisGone`) | invisible sections |
|---|---|---|
| /about | yes | **0** (unrevealed 0/22, hidden 0) |
| /print-on-demand | yes | **0** (0/11) |
| /infrastructure | yes | **0** (0/1) — [SHOT nav-infrastructure.png] fully populated |
| /newsroom | yes | **0** (0/8) |
| /contact | yes | **0** (0/3) |
| /legal/privacy | yes | **0** (0/1) |
| /legal/cookies | yes | **0** (0/1) |
| /legal/terms | yes | **0** (0/1) |
| /legal/accessibility | yes | **0** (0/1) |

Every reachable inner route: **0/N sections invisible after the homepage killAll.** Visual
corroboration: `nav-infrastructure.png` shows the full Infrastructure page (hero, FacilityBook,
capabilities, YouTube feed, gallery, certs, awards, footer) rendered after the killAll.

`/global-markets` and `/fulfilment` cannot be reached by a single homepage click (navigation
orphans, §2); their deep-link renders (§2) are clean (0 stranded reveals). The landmine is
dead for all click-reachable inner routes.

---

## 4. Content plumbing inventory

### 4a. Locale JSON (`src/locales/{en,fr,es}/`)

26 namespaces. Flattened key counts (arrays counted per index) [CODE]:

| namespace | top-level EN | EN keys | FR keys | ES keys | missing in FR | missing in ES |
|---|---|---|---|---|---|---|
| common | 7 | 16 | 16 | 16 | 0 | 0 |
| contact | 13 | 132 | 132 | 132 | 0 | 0 |
| cookie | 5 | 5 | 5 | 5 | 0 | 0 |
| educationalBooks | 5 | 47 | 47 | 47 | 0 | 0 |
| footer | 10 | 20 | 20 | 20 | 0 | 0 |
| fulfilment | 14 | 95 | 95 | 95 | 0 | 0 |
| globalMarkets | 5 | 25 | 25 | 25 | 0 | 0 |
| **home** | 5 | 59 | **60** | 59 | 0 | 0 |
| homeAwards | 10 | 57 | 57 | 57 | 0 | 0 |
| homeCases | 10 | 28 | 28 | 28 | 0 | 0 |
| homeCerts | 10 | 24 | 24 | 24 | 0 | 0 |
| homeInfraSection | 8 | 132 | 132 | 132 | 0 | 0 |
| homeProcess | 6 | 24 | 24 | 24 | 0 | 0 |
| homeProjects | 10 | 69 | 69 | 69 | 0 | 0 |
| homeSustain | 7 | 11 | 11 | 11 | 0 | 0 |
| homeTrust | 7 | 57 | 57 | 57 | 0 | 0 |
| homeTrustBelt | 2 | 7 | 7 | 7 | 0 | 0 |
| homeWwp | 9 | 28 | 28 | 28 | 0 | 0 |
| infrastructurePage | 10 | 91 | 91 | 91 | 0 | 0 |
| legal | 6 | 133 | 133 | 133 | 0 | 0 |
| nav | 22 | 22 | 22 | 22 | 0 | 0 |
| newsroom | 13 | 19 | 19 | 19 | 0 | 0 |
| ourStory | 9 | 80 | 80 | 80 | 0 | 0 |
| printOnDemand | 8 | 131 | 131 | 131 | 0 | 0 |
| tradeBooks | 13 | 81 | 81 | 81 | 0 | 0 |
| whatsapp | 1 | 1 | 1 | 1 | 0 | 0 |

**Full missing/extra key list (the ONLY discrepancy across all 26 namespaces):**
- **No EN key is missing in FR.** **No EN key is missing in ES.** **No ES key is extra.**
- `home`: FR has **one extra key not in EN** → `hero.book.rightPara[6]` (FR's
  `hero.book.rightPara` array has 7 entries `[0..6]`; EN and ES have 6 entries `[0..5]`).

i18n coverage is otherwise complete parity across EN/FR/ES.

### 4b. `public/site-assets/` inventory

**238 files, 72,804,796 bytes total (≈ 69.4 MiB)** [CODE] including READMEs, videos and the
PDF. The full per-file inventory (path | bytes | dimensions) was captured with `sharp`; a
directory→slot map from the code path-builders follows. Because the tree uses dynamic path
templates, "orphan" claims below are **candidates**, not confirmed-dead (per the brief).

**Directory → page / slot (traced from the code that builds the paths, not grep of filenames):**

| directory | page / slot | builder | files used |
|---|---|---|---|
| `homepage/hero/hero-main{,-fr,-es}.webp` | Home Hero art (language-keyed, EN fallback) | `Hero.jsx` HERO_ART | 3/3 used |
| `homepage/hero/prop-bookstack.webp`, `prop-plane.webp` | — | not referenced (Hero uses only hero-main*) | **ORPHAN candidate** |
| `homepage/brand/qfp-mark.png` | Nav + footer logo, POD css bg | SiteNav/CTAFooter/PrintOnDemand.css | used |
| `what-we-print/*.webp` (10) | Home "Formats and Categories" (WhatWePrint) | `WhatWePrint.jsx` PRODUCTS | 10/10 used |
| `homepage/stat-icons/stat-{book,box,clock,globe}.webp` | Home TrustStrips stat icons | `TrustStrips.jsx` | 4/4 used |
| `homepage/destinations/{africa-1,asia-2,europe-2}.jpg` | Home Projects "Worldwide Deliveries" cards | `Projects.jsx` DESTS | **3 of 11 used**; other 8 (`africa-2/3/4, asia-1/3, europe-1/3/4`) **ORPHAN candidates** |
| `homepage/globe/{earth-blue-marble.jpg,earth-topology.png,worldmap-dots.webp}` | Globe3D / GlobeFlyTo | `Globe3D.jsx`, `GlobeFlyTo.jsx` | used |
| `homepage/facility-book/*` (~40) | Home Infrastructure section + `/infrastructure` FacilityBook | `FacilityBook.jsx` `IMG(name)` — web-machines×9, sheetfed×8, binding×11, warehousing×6, head-office-01, book-stack, book-spread, binding-finishing-{diptych,trimmer} | **all used** |
| `homepage/certifications/{fsc,iso,sedex,star-export-house}.webp` | Home Certifications cards | `Certifications.jsx` | used (`iso.webp` used **twice**, see §5c) |
| `footer-certs/{fsc,iso-9001,iso-27001,sedex,star-export-house}.webp` | CTAFooter cert row | `CTAFooter.jsx` | 5/5 used |
| `homepage/awards/award-01..11.webp` | Home Awards + reused on About/Infrastructure | `Awards.jsx` | used |
| `homepage/cases/case-01..03.webp` | Cases section | `Cases.jsx` — **section gated OFF** (`SHOW_CASE_STUDIES=false`) | present but **not rendered** in current build |
| `homepage/process/process-artwork.webp` | Home ProcessVideo artwork | `ProcessVideo.jsx` | used |
| `homepage/video/how-we-work{.mp4,-poster.jpg}` | ProcessVideo; poster is also SEO default og:image | `ProcessVideo.jsx`, `Seo.jsx` | used |
| `homepage/video/facilities{.mp4,-poster.jpg,-thumb.webp,.vtt}` | Infrastructure section video dialog | `Infrastructure.jsx`, `index.css` | used |
| `homepage/products/product-01..10.webp` | — | no reference; `WhatWePrint.jsx` comment calls these "the old product-0N.webp" | **ORPHAN (superseded by `what-we-print/`)** |
| `about/team/*.webp` + `placeholder-portrait.svg` | About "Our Team" cards | `OurStory.jsx` TEAM_SLUGS | used |
| `about/gallery/*` (webp + 3 mp4) | About "Inside Quarterfold" gallery + lightbox | `src/assets/gallery/manifest.js` | used |
| `about/timeline/*.webp` (5) | — | no reference (the WAAPI timeline draws a dot-map, no photos) | **ORPHAN candidate** |
| `about/founder/founder-portrait.webp` | — | founder section removed (client) | **ORPHAN candidate** |
| `fulfilment/*` (card-0N, feature-0N, journey) | `/fulfilment` | `Fulfilment.jsx` BASE | used |
| `contact/facility-navy.webp` | `/contact` | `Contact.jsx` | used |
| `contact/map.webp` | — | no reference found | **ORPHAN candidate** |
| `print-on-demand/*` | `/print-on-demand` preview | PrintOnDemand | used |
| `infrastructure/facility/facility-01.webp`, `infrastructure/gallery/gallery-0N.webp` | `/infrastructure` | `InfrastructurePage.jsx` | used |
| `infrastructure/{awards,binding,certs,press,sheetfed,team,warehouse,video}/*`, plus `facility/{facility-02,03,book-spread,head-office-01}`, `gallery/walkthrough-poster.webp` | — | no reference found in `src` (.jsx/.js/.css) | **ORPHAN candidates** |
| `newsroom/gallery/*` (14) + `newsroom/video/press-run.mp4` | — | newsroom is Sanity-driven now; no local reference | **ORPHAN candidates (legacy)** |
| `documents/company-profile.pdf` | Footer "Download Company Profile" | `CTAFooter.jsx` | used |

**Orphaned files (candidates — no `src` reference via grep; dynamic templates mean these need
manual confirmation, not treated as ground truth):** `homepage/products/product-01..10.webp`
(10), `homepage/hero/prop-bookstack.webp`, `homepage/hero/prop-plane.webp`,
`homepage/destinations/{africa-2,africa-3,africa-4,asia-1,asia-3,europe-1,europe-3,europe-4}.jpg`
(8), `about/timeline/*.webp` (5), `about/founder/founder-portrait.webp`, `contact/map.webp`,
`newsroom/gallery/*` (14) + `newsroom/video/press-run.mp4`, `homepage/cases/case-0{1,2,3}.webp`
(present, section gated off), and the `infrastructure/{awards,binding,certs,press,sheetfed,team,
warehouse,video}` subfolders plus a few `infrastructure/facility` + `gallery` extras.

**Slots the code expects with no file present:** none detected among the slots traced above
(hero en/fr/es present; all `footer-certs` present; all team slugs + placeholder present; all
facility-book stage images present). Caveat: not every dynamic-builder folder was enumerated
image-by-image (see UNCERTAIN).

### 4c. README files that document these (paths only) [CODE]

`public/site-assets/README.md`, `public/site-assets/about/README.md`,
`public/site-assets/about/founder/README.md`, `public/site-assets/about/gallery/README.md`,
`public/site-assets/about/team/README.md`, `public/site-assets/about/timeline/README.md`,
`public/site-assets/contact/README.md`, `public/site-assets/documents/README.md`,
`public/site-assets/fulfilment/README.md`, `public/site-assets/homepage/README.md`,
`public/site-assets/homepage/awards/README.md`, `public/site-assets/homepage/brand/README.md`,
`public/site-assets/homepage/cases/README.md`,
`public/site-assets/homepage/certifications/README.md`,
`public/site-assets/homepage/destinations/README.md`,
`public/site-assets/homepage/facility-book/README.md`,
`public/site-assets/homepage/globe/README.md`, `public/site-assets/homepage/hero/README.md`,
`public/site-assets/homepage/process/README.md`, `public/site-assets/homepage/products/README.md`,
`public/site-assets/homepage/stat-icons/README.md`, `public/site-assets/homepage/video/README.md`,
`public/site-assets/infrastructure/README.md` (+ its `awards/binding/certs/facility/gallery/press/
sheetfed/team/video/warehouse` sub-READMEs), `public/site-assets/newsroom/README.md`,
`public/site-assets/newsroom/gallery/README.md`, `public/site-assets/newsroom/video/README.md`,
`public/site-assets/print-on-demand/README.md`. (38 README.md files in total under
`public/site-assets/`.)

---

## 5. Outstanding-item verification

### a. Footer social links [CODE + SHOT footer.png]
Wired in `CTAFooter.jsx`: exactly two, both `target="_blank"`:
- Instagram → `https://www.instagram.com/quarterfold_printabilities/`
- YouTube → `https://www.youtube.com/@quarterfoldprintabilities6000`

**LinkedIn and Facebook are NOT present** in the markup (code comment states they were removed
per client). The rendered footer shows only the Instagram and YouTube glyphs. [SHOT footer.png]

### b. Footer company-profile PDF [CODE + file analysis]
- Served path: `/site-assets/documents/company-profile.pdf` (footer "Download Company Profile"
  `<a download>`).
- The file **is a real, non-empty PDF**, not a 0-byte / HTML placeholder: 61,556 bytes, PDF
  1.4, **1 page**, Producer `Skia/PDF m149`, Creator `Chromium`, created `D:20260723001914`
  (2026-07-23) — i.e. a Chromium "Print to PDF". Its page body is **vector content**; the only
  embedded raster image is a 256×256 copy of the **QFP "Qf" logo** [SHOT pdf-img-1.png].
- I could **not fully rasterize the page** in this environment (poppler absent; Playwright's
  headless viewer downloaded rather than rendered it), so the *copy* of the profile could not be
  read to confirm it is the final intended document — see UNCERTAIN. What is certain: it is a
  genuine, freshly-generated single-page PDF carrying the brand logo, not an empty stub.

### c. Certification logos (homepage Certifications section) [CODE + SHOT iso.webp / footer.png]
Five, from `src/sections/Certifications.jsx`, all under
`/site-assets/homepage/certifications/`:
1. FSC → `fsc.webp` (244×291)
2. ISO 9001 → `iso.webp` (346×346)
3. ISO 27001 → `iso.webp` (346×346)  ← **same file as ISO 9001**
4. Sedex → `sedex.webp` (480×132)
5. Two Star Export House → `star-export-house.webp` (346×346)

**The two ISO icons still point at the same generic globe file** (`iso.webp`) — confirmed a
blue circular "ISO over a globe" mark [SHOT iso.webp]. Separately, the *footer* cert row uses a
distinct set (`footer-certs/iso-9001.webp` and `footer-certs/iso-27001.webp`); those two files
are **both 96×96 and both exactly 3,582 bytes** (byte-identical size ⇒ effectively duplicate
images), and render as the same blue ISO globe [SHOT footer.png].

### d. YouTube feed [CODE + SHOT]
- Static data file `src/data/youtube.js`, **no API key** (thumbnails from the predictable
  `i.ytimg.com` URL; embeds via `youtube-nocookie.com`). [CODE]
- Three video IDs: `RBH8Fl20UhI`, `C5zvmiujeig`, `Y0V-H3to8Jw`.
- Rendered feed shows all three thumbnails + a "Watch more on our YouTube channel" pill.
  [SHOT youtube-feed.png]
- Play/embed state (opened each lightbox against live YouTube):
  - `RBH8Fl20UhI` — **plays** ("High-Quality Flexi Binding for Educational Books…"). [SHOT youtube-player-1.png]
  - `C5zvmiujeig` — **embed-blocked**: "Video unavailable — Playback on other websites has been
    disabled by the video owner." [SHOT youtube-player-2.png]
  - `Y0V-H3to8Jw` — **plays** ("Power Meets Precision … KOMORI G-37P Perfector"). [SHOT youtube-player-3.png]

### e. Uploadcare on the Print-on-Demand form [CODE + SHOT uploadcare-widget.png]
- Public key `a6e0ef37b180782394f8` — **confirmed** (`UPLOADCARE_PUB_KEY`, PrintOnDemand.jsx:171).
- 10 MB cap — **confirmed**: `UPLOAD_MAX_BYTES = 10 * 1024 * 1024` (line 174).
- Auto-storing — **confirmed**: `fd.append('UPLOADCARE_STORE', 'auto')` (line 206).
- It is a **custom direct-to-Uploadcare uploader** (XHR to `upload.uploadcare.com/base/`,
  CDN `https://64frdhu94e.ucarecd.net/`), not the hosted widget. The control is **gated behind
  the "Request this book" form** (`reqOpen` state); once opened it renders "Attachment
  (optional) — PDF, JPG, PNG or ZIP, up to 10 MB each. Three files maximum. / Choose a file"
  (accepts `.pdf,.jpg,.jpeg,.png,.zip`, `multiple`). [SHOT uploadcare-widget.png]

### f. Sitemap [CODE]
`public/sitemap.xml` — 18 `<url>` entries. Compared to the §2 route inventory:
- Static pages present: `/`, `/about`, `/global-markets`, `/print-on-demand`, `/contact`,
  `/infrastructure`, `/fulfilment`, `/newsroom`, `/legal/{privacy,cookies,terms,accessibility}`.
  **All non-redirect routes are present.** The two redirect routes (`/educational-books`,
  `/trade-books`) are correctly excluded.
- Newsroom articles: 6 slugs — `printweek-power-100-2026`, `business-connect-print-industry`,
  `printweek-book-education-company-of-the-year`, `printweek-investment-2022`,
  `printweek-400000-books-a-day`, `assocham-excellence-in-education`.
- **These 6 exactly match the 6 currently-published Sanity posts** (§5h). The 11 unpublished
  drafts are correctly absent. **No missing or stale entries detected.** (The newsroom
  `<lastmod>` values are all `2026-07-22`, static.)

### g. robots.txt [CODE] — full contents:
```
# Quarterfold Printabilities — https://quarterfoldltd.com
# All crawlers are welcome, including AI assistants and answer engines.

User-agent: *
Allow: /

# AI assistants and answer engines (explicitly welcomed)
User-agent: GPTBot
Allow: /
User-agent: OAI-SearchBot
Allow: /
User-agent: ChatGPT-User
Allow: /
User-agent: ClaudeBot
Allow: /
User-agent: Claude-Web
Allow: /
User-agent: anthropic-ai
Allow: /
User-agent: PerplexityBot
Allow: /
User-agent: Perplexity-User
Allow: /
User-agent: Google-Extended
Allow: /
User-agent: Applebot-Extended
Allow: /
User-agent: Amazonbot
Allow: /
User-agent: Bytespider
Allow: /
User-agent: CCBot
Allow: /
User-agent: Meta-ExternalAgent
Allow: /
User-agent: cohere-ai
Allow: /
User-agent: DuckAssistBot
Allow: /

Sitemap: https://quarterfoldltd.com/sitemap.xml
```

### h. Sanity newsroom [CODE + live query]
- Project `z8o5rxfi`, dataset `production` — confirmed (`studio/projectConfig.js`).
- Read token referenced via **`VITE_SANITY_READ_TOKEN`** (`src/lib/sanity.js:18`,
  `import.meta.env.VITE_SANITY_READ_TOKEN`); the value lives only in `.env.local` (git-ignored)
  and is **not hardcoded** anywhere tracked (see §5k).
- Live `INDEX_QUERY` (`published == true && publishedAt <= now()`) returns **6 posts**
  (17 total documents; 11 unpublished drafts):

  1. 2026-06-15 — *Quarterfold's Nilesh Dhankani named to PrintWeek's Power 100 for 2026* (`printweek-power-100-2026`, Awards)
  2. 2024-07-01 — *Business Connect: Quarterfold Printabilities, revolutionising the print industry* (`business-connect-print-industry`, Press)
  3. 2024-01-15 — *Quarterfold wins PrintWeek's Book Education Company of the Year* (`printweek-book-education-company-of-the-year`, Awards)
  4. 2022-09-15 — *PrintWeek: bullish about print, Quarterfold embarks on investment* (`printweek-investment-2022`, Press)
  5. 2022-01-15 — *PrintWeek: how Quarterfold is producing 4,00,000 books a day* (`printweek-400000-books-a-day`, Press)
  6. 2017-11-15 — *ASSOCHAM names Quarterfold runner-up for Excellence in the Field of Education* (`assocham-excellence-in-education`, Awards)

  (Cards 4–6 are the three that render with no cover image — §2.)

### i. Structured data (JSON-LD) per rendered route [RUNTIME + CODE]
- `/` (Home): **Organization** + **WebSite** (2 blocks).
- `/about`: **BreadcrumbList**.
- `/global-markets`: **BreadcrumbList**.
- `/print-on-demand`: **BreadcrumbList**.
- `/infrastructure`: **BreadcrumbList**.
- `/newsroom`: **BreadcrumbList**.
- `/newsroom/:slug`: **NewsArticle**.
- `/fulfilment`: **BreadcrumbList**.
- `/contact`: **BreadcrumbList** + **FAQPage** (16 Q/A).
- `/legal/{privacy,cookies,terms,accessibility}`: **BreadcrumbList** (each).
- `*` (404): **none**.
- Note: `EducationalBooks.jsx` and `TradeBooks.jsx` define BreadcrumbList JSON-LD in code, but
  their routes are redirects, so that schema never emits.

### j. Meta tags per route (EN/default render) [RUNTIME]
`document.title` / `<meta name=description>` actually rendered:

| route | title (chars) | desc chars | flags |
|---|---|---|---|
| `/` | "Educational Book Printing Company India \| Quarterfold" (53) | 167 | **desc > 155** |
| `/about` | "Our Story \| Quarterfold Printabilities, Since 2014" (50) | 141 | ok |
| `/global-markets` | "Book Printing for US, UK and Global Publishers \| Quarterfold" (60) | 155 | at the 60/155 limits |
| `/print-on-demand` | "Print on Demand \| Quarterfold Printabilities" (44) | 144 | ok |
| `/infrastructure` | "Print Infrastructure \| Quarterfold Printabilities" (49) | 129 | ok |
| `/newsroom` | "Newsroom \| Quarterfold Printabilities" (37) | 144 | ok |
| `/newsroom/printweek-power-100-2026` | "Quarterfold's Nilesh Dhankani named to PrintWeek's Power 100 for 2026, Newsroom · Quarterfold Printabilities" (108) | 233 | **title > 60, desc > 155** (dynamic per article) |
| `/fulfilment` | "Warehousing & Fulfillment \| Quarterfold Printabilities" (54) | 140 | ok |
| `/contact` | "Contact Us \| Quarterfold Printabilities" (39) | 141 | ok |
| `/legal/privacy` | "Privacy Policy, Quarterfold Printabilities" (42) | 154 | ok |
| `/legal/cookies` | "Cookie Policy, Quarterfold Printabilities" (41) | 143 | ok |
| `/legal/terms` | "Terms of Use, Quarterfold Printabilities" (40) | 145 | ok |
| `/legal/accessibility` | "Accessibility Statement, Quarterfold Printabilities" (51) | 136 | ok |
| `/educational-books` (→ home) | same as `/` (53) | 167 | **duplicate of `/`** |
| `/trade-books` (→ home) | same as `/` (53) | 167 | **duplicate of `/`** |
| `*` 404 | "Page Not Found \| Quarterfold Printabilities" (43) | 145 | ok |

**Titles > 60 chars:** the newsroom article (108; article titles are dynamic).
**Descriptions > 155 chars:** `/` (167) and the newsroom article (233).
**Duplicate title+description:** `/`, `/educational-books`, `/trade-books` all render the home
title/description (the latter two because they redirect to `/`).

### k. Hardcoded secrets / API keys / tokens committed to the repo [GREP + git]
- **None committed.** `git ls-files` shows no tracked `.env`/secret/token file. The Sanity
  read-token value from `.env.local` does **not** appear in any tracked file (`git grep` of the
  token value = empty). The studio write token is documented as living only in
  `studio/.env` (git-ignored).
- The only key-like literals in source are **public by design**: the Uploadcare **public**
  key `a6e0ef37b180782394f8` (`PrintOnDemand.jsx`, safe to expose) and the Sanity `projectId`
  `z8o5rxfi` / dataset `production` (documented public). The Sanity read token is a deliberate
  read-only Viewer token injected at build time via `VITE_SANITY_READ_TOKEN` (ships in the
  client bundle by design, not committed).
- All other "token/secret/key" grep hits are CSS design-token comments (false positives).

---

## 6. Homepage hero framing

First load, 1536×743 DPR 1.25, above the fold, no scroll. [SHOT hero-abovefold.png (true first
load, cookie banner up) and hero-clean-abovefold.png (cookie dismissed); measurements RUNTIME]

Measured geometry:
- Viewport height (fold) = **743** CSS px.
- `#hero h1` (headline): top 193, bottom 396 — **fully visible**. Text = "AN / INTEGRATED
  GLOBAL BOOK / MANUFACTURING AND FULFILLMENT PARTNER" (line 1 gold "AN", line 2 cream, line 3
  gold). The subhead ("Engineering excellence in book manufacturing and seamless global
  fulfillment for publishers across the world.") is also fully visible.
- Hero art `#hero img` (`hero-main.webp`, natural 2400×1350): **top = 582, bottom = 1446,
  height = 864**. So the fold at 743 sits **161 px below the top of the hero art** — only
  **~161 / 864 ≈ 18.6 %** of the artwork is above the fold.
- Both CTA buttons ("Infrastructure", "Our global reach") sit at **top ≈ 1371, bottom ≈ 1425**
  — i.e. **~628 px BELOW the fold. Neither CTA button is visible above the fold on first load.**

What is actually visible above the fold [SHOT]:
- The nav bar, the full headline, the full subhead.
- Only the **top ~161 px** of the artwork: the dotted world-map sky and the **tops of the
  speech bubbles** ("Oh yes!", "WOW!…", "What?…"). With the cookie banner dismissed
  [SHOT hero-clean-abovefold.png] the **very crown (a few px of hair) of the two centre kids**
  (the black-haired boy in red, the blonde girl) just begins to appear at the fold line; the
  **two outer kids** (yellow-kurta girl far left, green-hoodie boy far right — who sit lower in
  the art) are **entirely below the fold**. The four heads are positioned exactly as described
  (centre two higher, outer two lower) [SHOT hero-main.webp], but essentially **none of the
  kids' faces crest the fold** — at most the hairline of the two centre kids.
- On true first load the cookie banner overlays the bottom ~100 px of the viewport, covering
  even that hairline sliver [SHOT hero-abovefold.png].

Fold position relative to the top of the hero art: **161 px** (art top y=582, fold y=743).

---

## 7. About page — right-hand video box

**There is no persistent right-hand video box on the current `/about` page.** [RUNTIME + SHOT
route-about.png]
- A DOM scan of `/about` returns **0 `<video>` elements** on load.
- `OurStory.jsx` renders: PageHero → lede → JourneyTimeline (`.prun`, a WAAPI dot-map timeline,
  no `<video>`) → MVV triptych → Team grid → Founder pull-quote → Gallery → Awards →
  Certifications. **None is a small right-hand video box.**
- The **only** video on the page is inside the **Gallery "See More" lightbox**: `OurStory.jsx`'s
  `Gallery` component renders `<video controls autoPlay muted playsInline>` for any manifest
  entry ending in a video extension, and the gallery manifest points at
  `/site-assets/about/gallery/` where three mp4s exist (`facility-tour.mp4`, `web-press.mp4`,
  `binding-line.mp4`). That `<video>` is created only after the user opens the lightbox — it is
  not present at rest and is not a persistent box.
- Since this build's main JS/CSS hashes are identical to the deployed build (§1), the deployed
  `/about` has the same structure. The "small right-hand video box" in the brief does not exist
  in the current/deployed About page; the closest wiring is the Gallery lightbox `<video>`
  above.

---

## 8. Mobile first-pass scan (390×844 DPR 3)

Every route deep-linked, scrolled, and screenshotted full-page. Per-route one-liners
(hOverflow / scrollWidth / scrollHeight / console errors from [RUNTIME]; visual from [SHOT]):

| route | verdict |
|---|---|
| `/` | **BROKEN: horizontal overflow** — page is 741 px wide vs 390 viewport (**+351 px**), ~351 px of dead space scrolls to the right. Content otherwise complete. [SHOT m-home.png] |
| `/about` | **BROKEN: same +351 px horizontal overflow** (741 px wide). Plus the animated "JOURNEY TIMELINE" band reads near-empty in static capture. [SHOT m-about.png] |
| `/infrastructure` | **BROKEN: same +351 px horizontal overflow** (741 px wide) — the full-page capture is literally 741 CSS-wide with content in the left 390 px and dead space right. FacilityBook renders. [SHOT m-infrastructure.png] |
| `/global-markets` | OK — no overflow (390). Renders; "Our Global Team — Details coming shortly" stub present. [SHOT m-global-markets.png] |
| `/print-on-demand` | OK — no overflow. Configurator collapses cleanly to one column. [SHOT m-print-on-demand.png] |
| `/newsroom` | OK — no overflow. 6 cards stack; older 3 still lack covers. [SHOT m-newsroom.png] |
| `/newsroom/:slug` | OK — no overflow, 0 errors. [RUNTIME] |
| `/fulfilment` | OK — no overflow; renders fully. [SHOT m-fulfilment.png] |
| `/contact` | OK — no overflow; form + FAQ stack fine. [SHOT m-contact.png] |
| `/legal/privacy` | OK — no overflow, 0 errors. [RUNTIME] |
| `/legal/cookies` | OK — no overflow, 0 errors. [RUNTIME] |
| `/legal/terms` | OK — no overflow, 0 errors. [RUNTIME] |
| `/legal/accessibility` | OK — no overflow, 0 errors. [RUNTIME] |
| `*` 404 | OK — no overflow, 0 errors. [RUNTIME] |

**Root cause of the horizontal overflow** [RUNTIME probe]: the unclipped element that defines
the 741 px page width on all three affected pages is the **Certifications carousel card**
(`ARTICLE.cert-card`, right edge = 741). Certifications is unique to exactly `/`, `/about`,
`/infrastructure` — the exact set that overflows — while every non-overflowing route omits it.
The site-wide cookie banner (`.ck-banner`) also stretches to 741 px, but since it is present on
*every* page yet only these three overflow, it is a **symptom** of the widened document, not the
cause. (The FacilityBook — the brief's flagged risk — renders on `/` and `/infrastructure` but
NOT on `/about`, which also overflows, so FacilityBook is not the cause; it was inspected and
renders. The Awards marquee and the TrustStrips ticker are clipped by their own scrollers and do
not extend the document.)

**Worst five (ranked):**
1. `/` — homepage horizontal overflow (+351 px); highest-traffic page.
2. `/infrastructure` — +351 px overflow.
3. `/about` — +351 px overflow + near-empty animated timeline band on mobile.
4. `/newsroom` — 3 of 6 cards render with empty cover panels (content, not layout).
5. `/global-markets` — "Our Global Team — Details coming shortly" placeholder block.

The Certifications carousel is the single component to fix to clear items 1–3.

---

## UNCERTAIN — could not determine, and why

- **company-profile.pdf actual copy.** Confirmed it is a real 1-page Chromium-generated PDF
  (vector body + embedded QFP logo), but I could not rasterize the full page in this
  environment (no poppler/pdftoppm; Playwright's headless viewer downloaded rather than
  rendered it; an `<iframe>`/`<embed>` render returned a blank viewer). So whether the page's
  text is the *final intended* company profile (vs. an interim draft) is unverified — the body
  text appears to be CID-encoded and did not extract as readable strings.
- **Per-file slot attribution for a few dynamic-builder folders.** `Fulfilment.jsx` (BASE) and
  parts of `InfrastructurePage.jsx` were not enumerated image-by-image; the `infrastructure/*`
  sub-folders flagged as orphan candidates in §4b have **no grep reference** but could
  conceivably be referenced from CSS I did not exhaustively read, or reserved for a slot not yet
  wired. Treated as candidates, not confirmed-dead, per the brief's caution about `site-assets/`.
- **Meta tags were audited in the EN (default) render only.** Titles/descriptions are localized
  (FR/ES differ via the in-app toggle); their FR/ES lengths were not separately measured, so the
  §5j overflow flags are for the English strings.
- **Newsroom missing cover images** — observed on the 3 oldest cards. Whether this is intended
  (those Sanity documents simply have no `coverImage`) or an oversight was not confirmed beyond
  the render; the query returns those posts without a `coverImage` asset.
- **About `.prun` timeline** — after scrolling it into the live viewport and waiting ~4 s, only
  the eyebrow, "JOURNEY TIMELINE" heading, dot-map and 5 pulsing markers were painted; the
  milestone cards (whose text IS in the DOM, 74 WAAPI animations running) were not visible at
  the captured frame. I could not confirm the full milestone sequence paints for a user; it is
  an animation-driven section, not a stranded reveal (DOM: 0 unrevealed, 0 hidden blocks).
- **YouTube play/block state** reflects YouTube's embed settings at test time (2026-08-09) over
  a live network; a video owner could change embedding later.
- **`/global-markets` and `/fulfilment` were not exercised via a homepage click-through in §3**
  because no in-app link to them exists (navigation orphans); their killAll behaviour is
  inferred from their clean deep-link renders, not from an actual home→click transition.
